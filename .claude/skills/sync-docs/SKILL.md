---
name: sync-docs
description: Full autonomous docs resync — runs bmad-document-project (deep scan) then patches any remaining stale sections by exploring src/ directly. Use when docs are significantly out of date.
triggers:
  - sync-docs
  - sync all docs
  - full docs sync
  - update all docs
  - resync docs
---

Follow the instructions in ./workflow.md
