[project]
name = "ps-projektas"

[llm]
provider = "claude"

[tools]
enabled = ["claude", "gemini", "cursor", "codex", "copilot"]

[limits]
max_messages_per_sync = 150
max_message_chars = 500
mind_max_lines = 150
